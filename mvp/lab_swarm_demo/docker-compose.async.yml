services:
  node:
    build:
      context: ../..
    image: blackswan-node:latest
    entrypoint: ["/app/mvp/lab_swarm_demo/node_entrypoint_async.sh"]
    environment:
      - BURN_RATE=0.01
      - FAILURE_PROB=0.0
      - GOSSIP_PORT=9777
      - TOTAL_NODES=4
      - PYTHONUNBUFFERED=1
      - LLM_MODEL=deepseek
      - GOSSIP_SIGNING_ENABLED=false
      - MEMORY_API_ENABLED=true
      - MARKET_MODE=web3
      - TEST_WEB3_SWAP_AMOUNT=0.001
      - TEST_WEB3_SWAP_SIDE=sell
      - PRICE_SCALE=10000
      - TRADING_SYMBOLS=WETH/USDC
      - LOG_LEVEL=DEBUG   # ← для диагностики (потом можно INFO)
      - INTERNET_RESEARCHER_ENABLED=true
      - TRADINGVIEW_WEBHOOK_ENABLED=true
      - TRADINGVIEW_WEBHOOK_PORT=8888
      - ORDERBOOK_ANALYSIS_ENABLED=false
      - HEDGE_ENABLED=true
      - HEDGE_RATIO=0.5
      - CAPITAL_ALERT_THRESHOLD=100.0
      # Секреты — через подстановку из .env (файл должен лежать рядом с этим compose-файлом)
      - WEB3_PRIVATE_KEY=${WEB3_PRIVATE_KEY}
      - BINANCE_TESTNET_API_KEY=${BINANCE_TESTNET_API_KEY}
      - BINANCE_TESTNET_API_SECRET=${BINANCE_TESTNET_API_SECRET}
      - ETHERSCAN_API_KEY=${ETHERSCAN_API_KEY}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
    ports:
      - "9777"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 0
      resources:
        limits:
          cpus: '0.8'
          memory: 2G
        reservations:
          cpus: '0.3'
          memory: 1G
    volumes:
      - ../../llama_cpp:/app/llama_cpp:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro