{
  "path": "src/risk_management",
  "description": "A dedicated module for comprehensive risk management. It would implement pre-trade risk checks (e.g., maximum drawdown limits, position sizing, exposure limits per asset/strategy, daily loss limits), post-trade monitoring, and potentially dynamic stop-loss/take-profit adjustments based on portfolio-level rules. This ensures the entire swarm operates within predefined risk parameters and prevents catastrophic losses.",
  "code_skeleton": "import logging\n\nclass RiskManager:\n    def __init__(self, config):\n        self.config = config\n        self.current_portfolio_value = 0.0\n        self.max_drawdown_limit = config.get('max_drawdown_limit', 0.1)\n        self.max_exposure_per_asset = config.get('max_exposure_per_asset', 0.25)\n        self.logger = logging.getLogger(self.__class__.__name__)\n\n    def update_portfolio_value(self, value):\n        self.current_portfolio_value = value\n\n    def pre_trade_check(self, order_request):\n        # Example: Check if order exceeds max exposure for asset\n        asset_exposure = self._calculate_current_exposure(order_request['symbol'])\n        if (asset_exposure + order_request['value']) > (self.current_portfolio_value * self.max_exposure_per_asset):\n            self.logger.warning(f\"Pre-trade check failed: Exceeds max exposure for {order_request['symbol']}\")\n            return False\n\n        # Example: Check portfolio-wide drawdown limit\n        # (Requires more complex state tracking, simplified here)\n        if self._is_exceeding_drawdown_limit():\n            self.logger.warning(\"Pre-trade check failed: Portfolio exceeding drawdown limit.\")\n            return False\n        \n        return True\n\n    def post_trade_monitor(self, filled_order):\n        # Log and monitor position, update risk metrics\n        self.logger.info(f\"Trade executed: {filled_order['symbol']} {filled_order['quantity']} @ {filled_order['price']}\")\n        # Trigger stop-loss/take-profit logic if needed\n\n    def _calculate_current_exposure(self, symbol):\n        # Placeholder: Retrieve current exposure for a given symbol from portfolio state\n        return 0.0 # Example value\n\n    def _is_exceeding_drawdown_limit(self):\n        # Placeholder: Implement logic to check if current portfolio drawdown exceeds limit\n        return False\n\n    def apply_position_sizing(self, strategy_signal):\n        # Example: Simple fixed fraction sizing based on signal strength\n        risk_per_trade = self.current_portfolio_value * self.config.get('risk_per_trade_fraction', 0.01)\n        # Further logic to determine position size based on volatility, signal, etc.\n        return risk_per_trade / 10 # Example: Convert risk to a rough share quantity"
}